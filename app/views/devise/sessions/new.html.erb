<<<<<<< HEAD
<div class="col-lg-6 mx-lg-auto">
  <div class="card shadow">
    <div class="card-header bg-rednav">
      <h2 class="card-title text-center text-white">Iniciar sesión</h2>
    </div>
        <div class="card-body text-center ">
              <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
                <div class="form-group mb-2">
                  <%= f.label :email %><br />
                  <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
                </div>

                <div class="form-group mb-2">
                  <%= f.label :password %><br />
                  <%= f.password_field :password, autocomplete: "current-password" %>
                </div>

                <% if devise_mapping.rememberable? %>
                  <div class="form-group mb-2">
                    <%= f.check_box :remember_me %>
                    <%= f.label :remember_me %>
                  </div>
                <% end %>

                <div class="form-group mb-2">
                  <%= f.submit "Iniciar Sesion",class: "btn bg-rednav", data: {turbo: false} %>
                </div>
              <% end %>

              <%= render "devise/shared/links" %>

        </div>
  </div>
</div>

=======

<div class="col-lg-6 mx-lg-auto">
  <div class="card shadow">
    <div class="card-header bg-rednav">
      <h2 class="card-title text-center text-white">Iniciar Sesión</h2>
    </div>
        <div class="card-body text-center ">
            <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <div class="field">
    <%= f.label "Correo Electrónico" %><br />
    <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
  </div>

  <div class="field">
    <%= f.label "Contraseña" %><br />
    <%= f.password_field :password, autocomplete: "current-password" %>
  </div>

  <% if devise_mapping.rememberable? %>
    <div class="field">
      <%= f.check_box :remember_me %>
      <%= f.label "Quieres que te recuerde" %>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit "Iniciar Sesión", :class=>"btn bg-rednav" %>
  </div>
<% end %>
        </div>
    </div>
</div>
>>>>>>> commit-huerfano
