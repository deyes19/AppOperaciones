<div class="container">
<nav class="navbar navsep navbar-expand-sm navbar-rednav bg-rednav rounded">
    <div class="container-fluid">
      <div class="navbar-collapse" id="mynavbar">
        <ul class="navbar-nav me-auto">
          <li class="nav-item">
          <i>
            <%= link_to " Nuevo Usuario", new_user_path, :class=>"nav-item nav-link bi bi-plus-square-dotted" %>
          </i>
          </li>
        </ul>
        <form class="d-flex">
          <input class="form-control me-2" type="text" placeholder="Usuario">
          <button class="btn btn-secondary" type="button">Buscar</button>
        </form>
      </div>
    </div>
  </nav>

<div class="row">
      <div class="col-md-8">
        <div class="tzona">
            <h1>Usuarios</h1>
        </div>

            <table class="table table-dark table-hover">
                  <tr class="text-dark">
                    <th>Rol</th>
                    <th>Nombre</th>
                    <th>Email</th>
                    <th>Acciones</th>
                  </tr>
                      <div id="users">
                        <% @users.each do |user| %>
                          <tr>
                            <td>
                              <%= user.role.name%>
                            </td>
                            <td>
                              <%= user.name %>
                            </td>
                            <td>
                              <%= user.email %>
                            </td>
                            <td>
                              <i class="separar">
                              <% if can? :destroy, User %>
                                <%= link_to "", user, :class=>"bi bi-eye-fill btn" %>
                                <%= button_to "", user, method: :delete, :class=>"bi bi-trash btn", :data=>{turbo_confirm:'Estás seguro?'}%>
                                <% end %>
                                <% if can? :edit, User %>
                                <%= link_to "", edit_user_path(user), :title=>"Editar", :class=>" bi bi-pencil-fill btn" %>
                                <% end %>
                              </i>
                            </td> 
                          </tr>
                        <% end %>
                      </div>
            </table>
        </div>
  </div>
</div> 