<%= form_with(model: zone) do |form| %>
  <% if zone.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(zone.errors.count, "error") %> prohibited this zone from being saved:</h2>

      <ul>
        <% zone.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <!--Se agrega campo para asignarle manualmente el ID a cada registro-->
 <div class="container mt-3">
  <div class="mb-3 mt-3">
    <%= form.label :id, :class=>"form-label", style: "display: block" %>
    <%= form.text_field :id, :class=>"form-control" %>
  </div>
  <div class="mb-3">
    <%= form.label :description, :class=>"form-label", style: "display: block" %>
    <%= form.text_field :description, :class=>"form-control" %>
  </div>

  <div>
    <%= form.submit "Actualizar información", :class=>"btn "  %>
  </div>
<% end %>
